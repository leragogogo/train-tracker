# Backend base URL — Vite proxies /departures requests here during development
VITE_API_URL=http://localhost:8000

# Milliseconds to wait after the user stops typing before firing a search request
VITE_DEBOUNCE_MS=300

# Minimum number of characters required to trigger a search
VITE_MIN_QUERY_LENGTH=3
